# LifeCalc Environment Configuration
#
# LifeCalc is a fully static, browser-only application.
# No backend environment variables are required.
# All data is stored in the browser's localStorage.
#
# Build: npm run build
# Output: out/
# Deploy: Upload the out/ directory to any static host (e.g. Cloudflare Pages).